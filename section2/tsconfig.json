{
  "compilerOptions": {
    "target": "ESNext", // 컴파일 결과 생성되는 자바스크립트 코드의 버전 결정, ESNext는 최신 자바스크립트를 의미함
    "module": "ESNext", // 컴파일 결과 생성되는 자바스크립트 코드의 모듈 시스템 결정
    "outDir": "dist", // tsc 사용시 js 파일들이 생성되는 곳, 컴파일 결과 생성되는 자바스크립트 파일들의 위치 결정
    "strict": true, // 엄격한 타입 검사 여부 설정 => 이게 최 상단
    "moduleDetection": "force", // 각각의 ts 파일을 독립적으로 판단, 모든 타입스크립트 파일(.ts)에 export 키워드를 자동으로 추가하여 격리된 모듈로 취급되도록 만드는 옵션
    "skipLibCheck": true, // 불 필요한 타입 정의 파일의 타입 검사 생략
  },
  "include": ["src"] // src 아래의 모든 ts 파일들을 컴파일, tsc로 컴파일 할 타입스크립트 파일의 범위 설정
}
